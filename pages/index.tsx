import type { NextPage } from "next";
import { BasicTable } from "../components/BasicTable";

const Home: NextPage = () => {
  return (
    <div>
      <BasicTable></BasicTable>
    </div>
  );
};

export default Home;
